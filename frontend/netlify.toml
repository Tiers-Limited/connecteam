# Netlify build and deploy settings for ConnectTeam frontend

[build]
  command   = "npm run build"
  publish   = "dist"
  base      = "/"

# SPA: all routes go to index.html (React Router)
[[redirects]]
  from    = "/*"
  to      = "/index.html"
  status  = 200
